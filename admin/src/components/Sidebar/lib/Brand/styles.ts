import {styled} from "styled-components";
import {PALETTE} from "styles";

export const BrandStyled = styled.div`
  strong {
    color: ${PALETTE["WHITE"]};
    font-size: 1.5rem;
    font-weight: 600;
  }
`;
