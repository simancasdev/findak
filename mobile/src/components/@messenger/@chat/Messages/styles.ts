import {StyleSheet} from "react-native";
import {HEIGHT_SCREEN} from "src/utils";

export const STYLES = StyleSheet.create({
  box: {
    height: HEIGHT_SCREEN / 1.2 - 60,
    paddingHorizontal: 15,
  },
});
