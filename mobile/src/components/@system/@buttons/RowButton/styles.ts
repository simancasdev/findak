import {StyleSheet} from "react-native";

export const STYLES = StyleSheet.create({
  row_button: {
    borderRadius: 5,
    paddingVertical: 10,
    paddingLeft: 15,
    paddingRight: 10,
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
  },
});
