import {StyleSheet} from "react-native";

export const STYLES = StyleSheet.create({
  options: {
    position: "absolute",
    top: 20,
    lef: 400,
    marginBottom: 8,
  },

  option: {
    backgroundColor: "red",
  },
});
